name: new_project107
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Update to a compatible version

flutter:
  uses-material-design: true

  assets:
    - assets/images/   # To'liq rasm fayllari mavjudligini tekshirib chiqing
